<Page x:Class="Warehouse1.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:viewmodels="clr-namespace:Warehouse1.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
      mc:Ignorable="d"
      Title="LoginPage">

    <Grid Background="{DynamicResource WindowBackground}">
        <materialDesign:Card Padding="32" Margin="20">
            <StackPanel VerticalAlignment="Center" Width="360">
                <TextBlock Text="ВХОД В СИСТЕМУ"
                           FontSize="24"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>

                <!-- Username (bind to VM.Username) -->
                <TextBox x:Name="UsernameBox"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Имя пользователя"
                         Margin="0,0,0,12"/>

                <!-- Password -->
                <PasswordBox x:Name="PasswordBox"
                             materialDesign:HintAssist.Hint="Пароль"
                             Margin="0,0,0,16"/>

                <Button Content="Войти"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        Padding="8"
                        HorizontalAlignment="Stretch"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Page>
